const { Shapes, Triangle, Square, Circle } = require('./shapes')

const shapePicker = (shape) => {
    if(shape === 'Triangle') {
        const triLogo = new Triangle(
            newLogo.fill,
            newLogo.text,
            newLogo.textFill
          );
    } else if(shape === 'Square'){
        const sqrLogo = new Square(
            
        )
    } else 
}